__pycache__/
*.pyc
data/*.db
# Allow .streamlit/config.toml (theme) but never secrets
.streamlit/secrets.toml
.venv/
venv/
.claude/settings.local.json
*.log

# Personal holdings — never commit. Use holdings.yaml.example as a template.
config/holdings.yaml
# Personal transaction logs (CSV uploads) — never commit.
config/*.csv

# Bundled tools (not source code)
tools/

# Test/build artifacts
.pytest_cache/
