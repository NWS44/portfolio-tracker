# Pin yfinance to the last known-good version. 1.4.0 silently returns no
# data on Streamlit Cloud (likely Yahoo anti-scraping change).
yfinance==1.3.0
# curl_cffi enables yfinance's anti-detection HTTP session (Yahoo blocks
# default requests UA on shared cloud IPs). Required by 1.x.
curl_cffi>=0.7.0
pandas>=2.0.0,<3.0.0
PyYAML>=6.0
streamlit>=1.30.0
plotly>=5.18.0
